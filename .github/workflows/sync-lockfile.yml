name: Sync lockfile

on:
  push:
    branches:
      - main
    paths:
      - pyproject.toml
  pull_request:
    paths:
      - pyproject.toml

jobs:
  sync:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      pull-requests: write

    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
          # PAT needed to push commits back to the PR branch
          token: ${{ secrets.GH_PAT }}
          # For PRs, check out the actual branch (not the merge commit)
          ref: ${{ github.head_ref || github.ref }}

      - name: Install uv
        uses: astral-sh/setup-uv@v4

      - name: Set up Python
        run: uv python install 3.11

      - name: Lock and sync
        run: |
          uv lock
          uv sync --all-extras

      - name: Commit updated lockfile (push to branch)
        run: |
          git config user.name  "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add uv.lock
          if git diff --cached --quiet; then
            echo "uv.lock unchanged, nothing to commit."
          else
            git commit -m "chore: update uv.lock"
            git push
          fi
